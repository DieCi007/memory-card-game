import styles from './home.module.scss';
import { useEffect } from 'react';

export const Home = () => {
    useEffect(() => {
    }, [])
    return (
        <div>

        </div>
    )
}

export default Home;
